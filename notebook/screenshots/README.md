# Langfuse Screenshots

Place screenshots from the Langfuse dashboard here after running the demo notebook:

1. `langfuse_traces.png` — List view showing all 5 agent invocations
2. `langfuse_trace_detail.png` — Detailed trace view with nested spans
